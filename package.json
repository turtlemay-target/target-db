{
    "private": true,
    "name": "target-db",
    "version": "0.1.0",
    "scripts": {
        "validate": "foy validate",
        "build": "foy build",
        "postinstall": "foy build"
    },
    "devDependencies": {
        "@types/node": "^14.11.8",
        "foy": "^0.2.1",
        "jsonschema": "^1.2.11",
        "target-db-schema": "turtlemay-target/target-db-schema"
    }
}
